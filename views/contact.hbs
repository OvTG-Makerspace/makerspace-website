<section class="contact">
    <div class="container">
        <div class="contact-hero">
            <h2>Kontakt</h2>
            <p>Schreiben Sie uns eine Nachricht – wir melden uns schnellstmöglich.</p>
        </div>

        {{#if success}}
            <div class="notice notice-success">{{success}}</div>
        {{/if}}
        {{#if error}}
            <div class="notice notice-error">{{error}}</div>
        {{/if}}

        <div class="contact-top-cta">
            <a class="btn help-mailto-btn" href="mailto:simonkorten09@gmail.com?subject=Kontakt%20OvTG%20Makerspace" rel="nofollow">
                {{!-- <span class="help-search-icon" aria-hidden="true">✉</span> --}}
                <span class="help-search-text">Zur E-Mail</span>
            </a>
        </div>
        {{!-- <div class="contact grid">
            <div class="contact-card contact-top">
                <div class="contact-top-inner">
                    <div class="contact-top-copy">
                        <h3>So erreichen Sie uns</h3>
                        <p>Wir freuen uns auf Ihre Ideen, Projekte und Fragen.</p>
                        <ul>
                            <li><strong>E-Mail:</strong> info@makerspace.local</li>
                            <li><strong>Standort:</strong> Beispielstraße 12, 00000 Musterstadt</li>
                            <li><strong>Öffnungszeiten:</strong> Mo–Fr 14:00–20:00</li>
                        </ul>
                    </div>
                </div>
            </div> --}}

            <div class="contact-card contact-help">
                <h3>Das Wichtigste auf einen Blick</h3>
                <div class="help-links">
                    {{#each helpArticles}}
                        <a class="help-link-card btn" href="{{href}}">
                            <h4>{{title}}</h4>
                            <p>{{desc}}</p>
                        </a>
                    {{/each}}
                </div>
            </div>
        {{!-- </div> --}}
    </div>
</section>
