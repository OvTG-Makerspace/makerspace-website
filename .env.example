# App
PORT=3000
ADMIN_EMAIL=admin@example.com
SMTP_FROM=no-reply@makerspace.ovtg.de

# App -> local relay
SMTP_HOST=smtp-relay
SMTP_PORT=25
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=

# Relay config
MAILNAME=makerspace.ovtg.de

# Option A (less safe): provide relay creds as env vars (avoid if possible)
RELAYUSERNAME=
RELAYPASSWORD=
